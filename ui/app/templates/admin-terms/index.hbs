{{#check-empty
  count=this.model.meta.count
  noResultsMessage="No terms were found matching these filters"
}}
  <Pagination
    @totalCount={{this.model.meta.count}}
    @singularName="term"
  />
  <AdminTermsList @terms={{this.terms}} />
{{/check-empty}}