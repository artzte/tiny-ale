<h2>
  {{term.attributes.name}} / {{term.attributes.schoolYear}}
</h2>

<AdminTermForm @model={{term}} @schoolYear={{schoolYear}} @schoolYears={{schoolYears}} @reportingBaseMonth={{reportingBaseMonth}} @save={{action "saveTerm"}} @reportError={{action "reportError"}} />
