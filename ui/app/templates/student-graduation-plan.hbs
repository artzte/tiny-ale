<h2>Graduation Plan</h2>

<GraduationPlan @student={{student}} @mappings={{mappings}} @requirements={{activeRequirements}} @creditAssignments={{creditAssignments}} @saveMapping={{action "saveMapping"}} @addMapping={{action "addMapping"}} @removeMapping={{action "removeMapping"}} />
