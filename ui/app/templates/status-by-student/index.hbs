<h2>Enrollments</h2>

<StatusByStudentEnrollments @student={{student}} @enrollments={{enrollments}} @enrollmentStatuses={{enrollmentStatuses}} @term={{term}} @getNotes={{route-action "getNotes"}} />  
