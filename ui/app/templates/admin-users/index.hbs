{{#check-empty
  count=model.meta.count
  noResultsMessage="No users were found with these filters"
}}
  <CountParagraph @result={{model}} @name="user" />
  <UsersList @users={{users}} @logins={{logins}} />
{{/check-empty}}
