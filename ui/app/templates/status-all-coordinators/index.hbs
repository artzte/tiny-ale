<Pagination
  @totalCount={{this.coordinators.length}}
  @singularName="coordinator"
/>
<StatusAllCoordinators
  @coordinators={{this.coordinators}}
  @students={{this.students}}
  @statuses={{this.statuses}}
  @term={{this.term}}
/>
