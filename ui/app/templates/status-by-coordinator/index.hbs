<CheckEmpty
  @count={{students.meta.count}}
  @noResultsMessage="No coordinatees"
>    
  <CountParagraph
    @result={{students}}
    @name="coordinatee"
  />

  <StatusByCoordinator
    @students={{students}}
    @term={{term}} @statuses={{statuses}}
  />
</CheckEmpty>
