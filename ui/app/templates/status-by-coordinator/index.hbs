<CheckEmpty
  @count={{students.meta.count}}
  @noResultsMessage="No coordinatees"
>    
  <Pagination
    @result={{students}}
    @name="coordinatee"
  />

  <StatusByCoordinator
    @students={{students}}
    @term={{term}} @statuses={{statuses}}
  />
</CheckEmpty>
