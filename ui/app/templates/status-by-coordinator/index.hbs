<CheckEmpty
  @count={{students.meta.count}}
  @noResultsMessage="No coordinatees"
>    
  <Pagination
    @totalCount={{students.length}}
    @singularName="coordinatee"
  />

  <StatusByCoordinator
    @students={{students}}
    @term={{term}}
    @statuses={{statuses}}
  />
</CheckEmpty>
