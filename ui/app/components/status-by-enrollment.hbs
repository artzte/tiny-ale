<section class="bg-gray-200 p-5">
  {{#each months as |month|}}
    <StatusByEnrollment::Row
      @enrollment={{@enrollment}}
      @month={{month}}
      @statusHash={{this.statusHash}}
      @notesHash={{notesHash}}
    />
  {{/each}}
</section>