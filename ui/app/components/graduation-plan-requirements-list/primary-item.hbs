<GraduationPlanRequirementsList::Row @requirement={{requirement}} @dropTarget={{dropTarget}} @setDrop={{setDrop}} @onDrop={{onDrop}} />
{{#each children as |child|}}
  <GraduationPlanRequirementsList::Row @indented={{true}} @requirement={{child}} @dropTarget={{dropTarget}} @setDrop={{setDrop}} @onDrop={{onDrop}} />
{{/each}}
