<GraduationPlanRequirementsList::Row @requirement={{this.requirement}} @dropTarget={{this.dropTarget}} @setDrop={{this.setDrop}} @onDrop={{this.onDrop}} />
{{#each this.children as |child|}}
  <GraduationPlanRequirementsList::Row @indented={{true}} @requirement={{child}} @dropTarget={{this.dropTarget}} @setDrop={{this.setDrop}} @onDrop={{this.onDrop}} />
{{/each}}
