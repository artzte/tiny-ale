<ul class="list-reset mr-4">
  {{#each this.filteredRequirements as |requirement|}}
    <GraduationPlan::RequirementsMapItem @requirement={{requirement}} @requirementType={{this.requirementType}} @mappingsHash={{this.mappingsHash}} @draggedCreditAssignment={{this.draggedCreditAssignment}} @editMapping={{this.editMapping}} @addMapping={{this.addMapping}} @removeMapping={{this.removeMapping}} />
  {{/each}}
</ul>