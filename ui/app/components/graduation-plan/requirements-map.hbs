<ul class="list-reset mr-4">
  {{#each filteredRequirements as |requirement|}}
    <GraduationPlan::RequirementsMapItem @requirement={{requirement}} @requirementType={{requirementType}} @mappingsHash={{mappingsHash}} @draggedCreditAssignment={{draggedCreditAssignment}} @editMapping={{editMapping}} @addMapping={{addMapping}} @removeMapping={{removeMapping}} />
  {{/each}}
</ul>