{{#if @onEdit}}
  <button
    data-button="edit"
    type="button"
    class="button"
    {{on 'click' (fn @onEdit)}}
    disabled={{@editing}}
  >
    Edit
  </button>
{{/if}}
{{#if @editing}}
  <button
    data-button="save"
    type="submit"
    class="button button-primary"
    disabled={{@disabled}}
  >
    Save
  </button>
  {{#if @onCancel}}
    <button
      data-button="cancel"
      type="button"
      class="button"
      {{on 'click' (fn @onCancel)}}
      disabled={{@disabled}}
    >
      Cancel
    </button>
  {{/if}}
{{/if}}