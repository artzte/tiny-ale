# update the task definition to take the latest tagged current
# update the ecs cluster so that the new task definition replaces the old
# it does not seem like the number of running instances needs to be replaced; it seems like eventually, the new replace the old
